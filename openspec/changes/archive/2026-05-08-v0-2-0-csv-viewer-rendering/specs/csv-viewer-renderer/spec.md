## ADDED Requirements

### Requirement: CSV viewer rendering is migrated to KDV

KDR SHALL NOT implement CSV viewer rendering after the KDV responsibility boundary is adopted.

#### Scenario: CSV viewer work starts

- **WHEN** CSV viewer rendering work is planned
- **THEN** the implementation belongs to KDV
- **THEN** KDR does not add CSV viewer APIs, fixtures, or CLI entrypoints
