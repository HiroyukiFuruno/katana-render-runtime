# lefthook.yml
no_tty: true
output:
  - failure

pre-commit:
  parallel: false
  commands:
    fmt-check:
      glob: "{*.rs,*.toml,*.lock,Justfile}"
      run: just fmt-check
    lint:
      glob: "{*.rs,*.toml,*.lock,Justfile}"
      run: just lint
    ast-lint:
      glob: "{*.rs,*.toml,*.lock,Justfile}"
      run: just ast-lint

pre-push:
  parallel: false
  commands:
    dispatch:
      run: bash scripts/hooks/pre-push.sh "{1}" "{2}"
      use_stdin: true

pre-pr:
  commands:
    release-openspec-archive:
      run: bash scripts/release/check-openspec-release-archive.sh
      only:
        - ref: release/v*
